

function App() {
  //note-> //in this function you can use the any kind of calculation 
  // at the end of the day that is a function so you can 
  // evaluate any such of thing you can simply return the 
  // variable in inside is curleybraces{variable}-> so on
  //in return you can't evaluate any kind of function of 
  //calculation so simply return the variable or any function 
 const user="Pradip Mondal"

return (
   <h1>Deatils  {user}</h1> 
  )
};

export default App
